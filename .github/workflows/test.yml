name: Testing CI
on:
  push:
  pull_request:
    branches:
    - main

jobs:
  test-erato-gb:
    strategy:
      matrix:
        os: [ ubuntu-latest ]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: goto-bus-stop/setup-zig@v2
      - run: zig build test

